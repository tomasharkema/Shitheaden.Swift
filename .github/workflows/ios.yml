name: Swift

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Compile
        run: xcodebuild -scheme ShitheadenApp -configuration Release
